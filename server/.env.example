# Server
PORT=3001
NODE_ENV=development

# MongoDB
MONGODB_URI=mongodb://localhost:27017/organizer

# Authentication
JWT_SECRET=change-this-to-a-secure-random-string

# CORS (optionnel, default: *)
# CORS_ORIGIN=http://localhost:5173

# Dev mode - skip auth (NEVER in production!)
DEV_SKIP_AUTH=true

# Qdrant vector database
QDRANT_URL=http://localhost:6333

# Note: Les clés API (Anthropic, OpenAI) sont dans agent-config.json
# Voir agent-config.example.json
